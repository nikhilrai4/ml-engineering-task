def run_beam(
    method: str,
    pool_over: str,
):
    raise NotImplementedError(
        "Fill this in with a beam pipeline replicating the steps done in run_multiprocess found in local.py"
    )
